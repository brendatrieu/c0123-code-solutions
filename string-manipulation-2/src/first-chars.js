/* exported firstChars */

/** PSEUDOCODE:
 *  Define a function 'firstChars' with two parameters: 'length', 'string'.
 *    Return 'string' sliced from index 0 up to index 'length'
 */

/**
 * CODE IMPLEMENTATION:
 */

function firstChars(length, string) {
  return string.slice(0, length);
}
