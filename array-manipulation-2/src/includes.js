/* exported includes */

/**
 * PSUEDOCODE:
 * Define a function 'includes' with two parameters: 'array', 'value'.
 *  Return if the index of the value in array is not equal to -1.
 */

/**
 * CODE IMPLEMENTATION:
 */

function includes(array, value) {
  return array.indexOf(value) !== -1;
}
