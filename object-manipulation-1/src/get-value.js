/* exported getValue */

/** PSEUDOCODE:
 *  Define a function 'getValue' that takes two parameters: 'object', 'key'.
 *    Return the value of the object at the key property.
 */

/**
 * CODE IMPLEMENTATION:
 */

function getValue(object, key) {
  return object[key];
}
