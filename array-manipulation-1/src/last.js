/* exported last */

/** PSEUDOCODE:
 *  Define a function called 'last' that takes in one parameter 'array'.
 *    Return the element at the last index of the array.
 */

/**
 * CODE IMPLEMENTATION
 */

function last(array) {
  return array[(array.length - 1)];
}
