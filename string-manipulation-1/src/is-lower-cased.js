/* exported isLowerCased */

/**
 * PSEUDOCODE:
 * Define a function called 'isLowerCased' that takes in one parameter 'word'.
 *  Return if 'word' lowercased is strictly equal to 'word'.
 **/

/**
 * CODE IMPLEMENTATION:
 */

function isLowerCased(word) {
  return word.toLowerCase() === word;
}
